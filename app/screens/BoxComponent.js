import React from "react";
import { StyleSheet, Button, ImageBackground, Text, View } from "react-native";

function BoxComponent({ navigation }) {
  return (
    <View>
      <Text>Let's Play</Text>
    </View>
  );
}

const styles = StyleSheet.create({
    container: {
        flex: 1
    }
});

export default BoxComponent;
